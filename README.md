# Project Kanaken System

This project has been migrated from a legacy Vue.js setup to a modern **Next.js** framework with **TypeScript** and **Biome**.

## Technical Stack

- **Framework:** [Next.js](https://nextjs.org/) (App Router)
- **Language:** [TypeScript](https://www.typescriptlang.org/)
- **Linting & Formatting:** [Biome](https://biomejs.dev/)
- **Styling:** CSS Modules & Global CSS

## Getting Started

First, install the dependencies:

```bash
npm install
```

Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Available Scripts

- `npm run dev`: Starts the development server.
- `npm run build`: Builds the application for production.
- `npm run start`: Starts the production server.
- `npm run lint`: Checks for linting and formatting issues using Biome.
- `npm run format`: Formats the codebase using Biome.
- `npm run lint:fix`: Automatically fixes linting issues where possible.

## Project Structure

- `app/`: Next.js App Router (Pages, Layouts, Styles).
- `public/`: Static assets (images, legacy website files).
- `document/`: Project documentation (`whatis.md`, original `README.md`).
- `biome.json`: Configuration for Biome.
- `tsconfig.json`: Configuration for TypeScript.

## Documentation

The original project documentation can be found in the `document/` directory.
